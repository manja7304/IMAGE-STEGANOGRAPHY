<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Steganography Coder</title>

    <!-- Bootstrap 5 -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <!-- Animate.css -->
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css"
    />
    <!-- Google Font: Space Grotesk -->
    <link
      href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;600;700&display=swap"
      rel="stylesheet"
    />

    <style>
      :root {
        --bg-dark: #0d1117;
        --card-bg: #161b22;
        --text: #f0f6fc;
        --green: #00ff9f;
        --accent: #39ff14;
        --font-main: "Space Grotesk", sans-serif;
      }

      html,
      body {
        background-color: var(--bg-dark);
        color: var(--text);
        font-family: var(--font-main);
        margin: 0;
        padding: 0;
        overflow-x: hidden;
        cursor: crosshair;
      }

      h2,
      h4 {
        font-weight: bold;
        color: var(--accent);
        text-shadow: 0 0 20px var(--accent);
      }

      .navbar {
        background: linear-gradient(145deg, #0d1117, #1a1f27);
        border-bottom: 2px solid;
        animation: borderRGB 3s infinite linear;
      }

      @keyframes borderRGB {
        0% {
          border-color: rgb(255, 0, 0);
        }
        33% {
          border-color: rgb(0, 255, 0);
        }
        66% {
          border-color: rgb(0, 0, 255);
        }
        100% {
          border-color: rgb(255, 0, 0);
        }
      }

      .navbar-brand {
        font-size: 2.2rem;
        color: var(--green);
        text-shadow: 0 0 20px var(--green);
      }

      .nav-link {
        color: #fff;
        margin: 0 10px;
        font-weight: 600;
        transition: all 0.3s ease;
        position: relative;
        text-shadow: 0 0 10px var(--green);
      }

      .nav-link::after {
        content: "";
        display: block;
        width: 0;
        height: 2px;
        background: var(--green);
        transition: width 0.3s ease;
        position: absolute;
        bottom: 0;
        left: 0;
      }

      .nav-link:hover::after {
        width: 100%;
      }

      .card {
        background-color: var(--card-bg);
        border-radius: 20px;
        padding: 25px;
        margin-top: 30px;
        box-shadow: 0 0 40px rgba(0, 255, 159, 0.3);
        transition: transform 0.3s ease;
        backdrop-filter: blur(4px);
      }

      .card:hover {
        transform: translateY(-5px);
        box-shadow: 0 0 60px rgba(0, 255, 159, 0.6);
      }

      .btn {
        transition: all 0.3s ease;
        font-weight: bold;
        font-family: var(--font-main);
      }

      .btn-glow {
        background-color: var(--green);
        border: none;
        color: #000;
        box-shadow: 0 0 20px var(--green);
      }

      .btn-glow:hover {
        transform: scale(1.05);
        box-shadow: 0 0 30px var(--green);
      }

      input[type="text"],
      input[type="file"] {
        background-color: #21262d;
        color: var(--text);
        border: 1px solid #30363d;
        font-family: var(--font-main);
        box-shadow: 0 0 20px rgba(0, 255, 159, 0.2);
      }

      .footer {
        text-align: center;
        margin: 40px 0 10px;
        font-size: 0.9rem;
        color: #888;
        text-shadow: 0 0 10px #888;
      }

      code,
      pre {
        font-family: var(--font-main);
      }
    </style>
  </head>

  <body class="animate__animated animate__fadeIn">
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg shadow-sm">
      <div class="container-fluid justify-content-between px-4">
        <a class="navbar-brand" href="#">🕵️ Image Steganography</a>
        <div>
          <a class="nav-link d-inline" href="#home">Home</a>
          <a class="nav-link d-inline" href="#encode">Encode</a>
          <a class="nav-link d-inline" href="#decode">Decode</a>
        </div>
      </div>
    </nav>

    <div class="container">
      <!-- Home -->
      <div id="home" class="card animate__animated animate__fadeInUp">
        <h2>🏠 Welcome !!!</h2>
        <p>
          Use the <strong>Encode</strong> tab to hide messages inside images.
          <br />
          <strong>How to Encode:</strong> Enter your secret message, select a
          PNG image, and click on "Encode". The message will be hidden in the
          image and you can download the encoded image.
        </p>
        <p>
          Or go to <strong>Decode</strong> to extract messages from stego
          images.
          <br />
          <strong>How to Decode:</strong> Upload an image that has a hidden
          message, and click on "Decode". The secret message will be extracted
          from the image and displayed.
        </p>
      </div>

      <!-- Encode -->
      <div id="encode" class="card animate__animated animate__fadeInUp">
        <h2>🛠️ Encode Message</h2>
        <form method="post" enctype="multipart/form-data" action="/encode">
          <div class="mb-3">
            <label for="secret_message" class="form-label"
              >Secret Message:</label
            >
            <input
              type="text"
              id="secret_message"
              name="secret_message"
              class="form-control"
              placeholder="Enter your secret message"
              required
            />
          </div>
          <div class="mb-3">
            <label for="image" class="form-label">Select PNG Image:</label>
            <input
              type="file"
              id="image"
              name="image"
              accept="image/png"
              class="form-control"
              required
            />
          </div>
          <button type="submit" class="btn btn-glow">🔐 Encode</button>
        </form>
        <small class="text-muted d-block mt-2"
          >Only .PNG format is supported.</small
        >
      </div>

      <!-- Encode Result -->
      {% if img_path and message %}
      <div class="card animate__animated animate__fadeInUp">
        <h4>✅ Encoded Result</h4>
        <p><strong>Encoded Message:</strong> {{ message }}</p>
        <img
          src="{{ img_path }}"
          alt="Encoded Image"
          class="img-fluid rounded border mb-3"
        />
        <a href="{{ img_path }}" download class="btn btn-outline-success">
          ⬇️ Download Image
        </a>
      </div>
      {% endif %}

      <!-- Decode -->
      <div id="decode" class="card animate__animated animate__fadeInUp">
        <h2>🧪 Decode Message</h2>
        <form method="post" enctype="multipart/form-data" action="/decode">
          <div class="mb-3">
            <label for="image" class="form-label"
              >Upload Image to Decode:</label
            >
            <input
              type="file"
              id="image"
              name="image"
              accept="image/*"
              class="form-control"
              required
            />
          </div>
          <button type="submit" class="btn btn-glow">🔓 Decode</button>
        </form>
      </div>

      <!-- Decode Result -->
      {% if secret %}
      <div class="card animate__animated animate__fadeInUp">
        <h4>🕵️ Decoded Message</h4>
        <p><strong>Secret:</strong> {{ secret }}</p>
      </div>
      {% endif %}

      <!-- Footer -->
      <div class="footer">
        🚀 Built by <strong>Manjunath K G</strong> — BCA Final Year Project
        (2025)
      </div>
    </div>
  </body>
</html>
